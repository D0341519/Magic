<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.user8856.magic">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/magicapp"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".gamestart" />
        <activity android:name=".Card_5" />
        <activity android:name=".nextcard5" />
        <activity android:name=".Card_4" />
        <activity android:name=".Allactivity"></activity>
    </application>

</manifest>